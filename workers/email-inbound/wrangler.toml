name = "email-inbound"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Secrets (set via: wrangler secret put INBOUND_WEBHOOK_URL)
# INBOUND_WEBHOOK_URL  — https://beyondopenrate.com.au/api/deliverability/inbound
# WORKER_SECRET        — same value as CLOUDFLARE_WORKER_SECRET in Vercel
